# requirements.txt - Python Dependencies for NEMSU Lost and Found System
# Install with: pip install -r requirements.txt

flask==3.0.0                  # Core web framework (MVC routing, templating)
requests==2.31.0              # HTTP requests for Google OAuth + Gemini AI API
oauthlib==3.2.2               # OAuth 2.0 library for Google login
requests-oauthlib==1.3.1      # OAuth integration for the requests library
Werkzeug==3.0.1               # WSGI utilities (used internally by Flask)
gunicorn==21.2.0              # Production WSGI server (used on Render)

# Gmail notifications (no extra install needed — uses Python stdlib smtplib)
# Configure via environment variables:
#   GMAIL_SENDER_EMAIL   your Gmail address
#   GMAIL_APP_PASSWORD   16-char Gmail App Password
#   APP_BASE_URL         (optional) deployment base URL for email links

# ── Gemini AI Chatbot ─────────────────────────────────────────────────────
# No extra packages needed — uses the requests library (already above).
# Get a FREE API key at: https://aistudio.google.com/app/apikey
# Set GEMINI_API_KEY environment variable to enable the AI support chatbot.

# ── Turso (free cloud SQLite) ─────────────────────────────────────────────
# Set TURSO_DATABASE_URL + TURSO_AUTH_TOKEN env vars to activate.
# Without them the app uses local SQLite automatically.
# Get credentials at https://turso.tech (free tier: 500 MB, 1 B reads/month).
libsql-experimental